# Changelog

桌面固定 **6 格**：每加一个应用就得换掉一个，所以这份记录里「X 取代 Y」的写法会反复出现。
被换下的应用不删，改名成 `*.ino.bak` 留在 `firmware/cores3_shell/` 里（arduino-cli 只编译 `.ino`）。

## 文档 — 双语 + 音乐致谢（无固件版本变化）
- README、NOTICE 和四个 docs 页面全部改成**中英双语**（原来只有 README 第 8 节一段英文简介）。
  页面右上角一组 `中文 / EN` 切换按钮，纯前端、`localStorage` 记住选择、不另生成第二份文件；
  打印时（`@media print`）语言条自动隐藏。
- 硬件组装手册的英文版复用中文版内嵌的同一份产品图（脚本按 `data-img="N"` 取中文版第 N 张 `src`），
  所以 7 张 base64 图不会在文件里存两遍。
- 音乐遥控的整体思路参考了 `isalicema/m5-faces-core-s3`（MIT），致谢写进了四处：
  `firmware/cores3_shell/app_music.ino` 文件头、NOTICE 第 6 节（中英）、README 第八章（中英），
  以及速查卡页面的音乐面板。

## v0.7.1 — 游戏静音开关
- 俄罗斯方块与 2048 各加一个静音开关：`M` 键或点屏上的按钮都能切，状态存 NVS（`gmute`），掉电不丢。
- 开关两边共用一档（一个开关管两个游戏）。打开声音时会「嘀」一声确认，关掉时当然不出声。
- 顺手修的：2048 盘面最后一行被底部提示行的 `clearArea` 啃掉了；「下一个」标签在中文下是空白
  （画它之前调的是 ASCII 点阵字体）。

## v0.7.0 — 游戏（俄罗斯方块 + 2048）取代系统信息
- 新增 `app_games.ino`：菜单 + 两个游戏。
  - **俄罗斯方块**：10×18 盘面，7 种方块（旋转态预计算），消行 100/300/500/800 × 等级，每 10 行升一级。
    贴墙也转得动（±1 格 wall kick）；下落只重画「擦 4 格 + 画 4 格」。
  - **2048**：4×4，最高分存 NVS（只在终局和退出应用时落盘，不每次合并都写 flash）。
- 下线 `app_sysinfo.ino` → `.bak`。

## v0.6.2 — 离线笔记带书写时间
- 离线补发的笔记，时间戳记的是**书写时间**而不是送达时间（离开编辑区那一刻就打戳）。
- 连上 Mac 端 Bridge 时自动对时 —— RTC（BM8563）没有后备电池，不对时时间会飘。
- 入队失败（队列满 32 条 / fs 不可用）时不再清空编辑区。

## v0.6.1 — 静态界面不再每秒重绘
- 界面上没有「自己会变」的内容时不挂 1Hz tick，改成事件驱动 + 标脏；只有时钟/倒计时/方块下落
  这种真动态的才 tick，而且只重画那一块。修掉了「整屏每秒重绘 → 肉眼看着一闪一闪」。
- 配套：局部重画前先 `clearArea`（数字位数变短会留残影）；「要重画」和「要存盘」拆成两个标志。

## v0.6.0 — 记事本取代时钟
- 新增 `app_notepad.ino`：Keyboard3 打字 → BLE notify → Mac 端 Bridge 追加进一个 markdown 文件。
  停手 2 秒存草稿进 LittleFS；发不出去的进队列（32 条）自动补发。
- 下线 `app_clock.ino` → `.bak`。
- 文件系统从 FFat 换成 **LittleFS**（分区 subtype `0x83`）—— FFat 在这块板上能挂载但写不了文件。

## v0.5.1 — 封面能显示 + 只重画进度条
- 音乐应用：封面 JPEG 能正常解码显示了；进度条单独重画，不再整屏刷。
- 新增串口 `time` 命令做 RTC 对时。

## v0.5.0 — 音乐遥控取代传感器
- 新增 `app_music.ino`：HID 媒体键控制 Mac 播放，并通过 **BLE 自定义 GATT 服务**把歌名/歌手/专辑/
  进度/封面回传到屏上（不用 Wi-Fi、不依赖局域网）。
- 下线 `app_sensors.ino` → `.bak`。

## v0.4.x — 控制器稳定化（这一段踩坑最密）
- `v0.4.9`：`del` 键是 `0x7F` 不是 `0x08`（Keyboard3 上标 del 的那颗），接上取消。
- `v0.4.8`：新建对话后的「补按焦点」默认关掉 —— Tab 遍历 / WAKE 档实测全部失败，不补按反而稳。
- `v0.4.6`：发给 Mac 的删除键从 `KEY_DELETE`(0x4C) 改成 `KEY_BACKSPACE`(0x2A)。
- `v0.4.5`：取消键 = `ESC` + 清空草稿（两连击）。
- `v0.4.4`：取消键改发 `ESC`（对齐 VibeKey）。
- `v0.4.3`：查清「焦点在不在输入框」是整套控制器的命门 —— 确认和清草稿都要求焦点在输入框内。

## v0.3.0 — 双语
- 中英两套文案表（`i18n.h` / `i18n.ino`），两表各有 `static_assert` 跟 `STR_COUNT` 对账，
  漏翻译一个就编译不过。
